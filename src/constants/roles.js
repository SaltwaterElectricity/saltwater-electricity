/**
 * CENTRALIZED ROLE CONSTANTS
 * Single source of truth for Role-Based Access Control (RBAC) across the SmartAqua platform.
 */
export const ROLES = Object.freeze({
  SUPER_ADMIN: "superAdmin",
  ADMIN: "admin",
  RESIDENT: "resident",
});
